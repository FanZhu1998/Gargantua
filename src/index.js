// Reusable public API. Bundle with loaders for .frag and .vert set to text.
export { createSimulator } from './simulation/simulator.js';
export { mountBlackHoleApp } from './ui/app.js';
export { DEFAULT_STATE, PARAMETERS, PRESETS, QUALITY_MODES } from './simulation/state.js';
